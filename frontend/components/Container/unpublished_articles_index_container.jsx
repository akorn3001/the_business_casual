import { connect } from 'react-redux';
import { withRouter } from 'react-router-dom';
import UnpublishedArticlesIndex from '../Presentational/unpublished_articles_index';

// const mapStateToProps = state => {
//   return {
//
//   };
// };

// const mapDispatchToProps = dispatch => {
//   return {
//
//   };
// };

export default connect(null, null)(UnpublishedArticlesIndex);
